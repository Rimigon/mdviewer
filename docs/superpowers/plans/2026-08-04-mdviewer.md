# MD Viewer Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Десктоп-приложение на Electron для красивого просмотра Markdown-файлов в стиле GitHub: GFM, Mermaid, KaTeX, TOC, поиск, тёмная/светлая тема, переключатель «рендер/исходник», навигация по папке, ассоциация `.md` в Windows.

**Architecture:** Electron (main-процесс владеет ФС через IPC; preload — тонкий contextBridge-мост; renderer — React SPA). Рендер Markdown — конвейер unified (remark/rehype) в чистой функции `renderMarkdown`, результат вставляется через `dangerouslySetInnerHTML`; пост-обработка в DOM (картинки, Mermaid, TOC, поиск) — эффекты React. Темы — динамическая инъекция CSS (`?inline`).

**Tech Stack:** Electron, React, TypeScript, electron-vite, Vite, unified + remark-parse/gfm/math/rehype + rehype-katex/highlight/slug/autolink-headings/raw/sanitize/stringify, github-markdown-css, highlight.js, katex, mermaid, electron-builder (NSIS), vitest + jsdom.

## Global Constraints

- Windows — основная платформа (реестровые ассоциации, NSIS-установщик).
- Пути в плане заданы относительно корня проекта: `C:/Users/nikit/Desktop/mdviewer/`.
- Безопасность: `contextIsolation: true`, `nodeIntegration: false`, `sandbox: true`; HTML проходит `rehype-sanitize` (скрипты и `on*`-атрибуты удаляются).
- Схема sanitize: `defaultSchema` из rehype-sanitize, дополненная `className` и `style` для `*` (нужно KaTeX/hljs/autolink-headings). Протокол `file:` в `src` НЕ разрешаем — относительные картинки резолвятся пост-рендером в renderer.
- TOC — заголовки H1–H4.
- Поиск — по рендеренному тексту, подсветка `<mark>`.
- React 18 или 19 (обе совместимы с кодом плана); остальные зависимости — latest на момент установки (ожидаемые мажоры указаны в каждом task).
- Каждый task заканчивается зелёными тестами (где есть) и коммитом.

---

### Task 1: Scaffold проекта (electron-vite + React + TS)

**Files:**

- Create: `package.json`
- Create: `tsconfig.json`, `tsconfig.node.json`, `tsconfig.web.json`
- Create: `electron.vite.config.ts`
- Create: `.gitignore`
- Create: `src/main/index.ts` (скелет окна)
- Create: `src/preload/index.ts` (пустой мост)
- Create: `src/renderer/index.html`
- Create: `src/renderer/src/main.tsx`, `src/renderer/src/App.tsx`, `src/renderer/src/env.d.ts`
- Test: нет автотестов (ручная проверка: окно открывается)

**Interfaces:**

- Consumes: —
- Produces: структура проекта, `npm run dev` открывает окно с надписью «MD Viewer»

- [ ] **Step 1: package.json + зависимости**

`package.json` (main указывает на `./out/main/index.js`, скрипты):

```json
{
  "name": "mdviewer",
  "version": "0.1.0",
  "description": "Красивый просмотрщик Markdown в стиле GitHub",
  "main": "./out/main/index.js",
  "private": true,
  "scripts": {
    "dev": "electron-vite dev",
    "start": "electron-vite preview",
    "build": "electron-vite build",
    "build:win": "electron-vite build && electron-builder --win",
    "test": "vitest run",
    "test:watch": "vitest",
    "register-assoc": "node scripts/register-assoc.mjs",
    "unregister-assoc": "node scripts/unregister-assoc.mjs"
  }
}
```

Установка (ожидаемые мажоры): `npm i react react-dom` (18/19), затем dev: `npm i -D electron electron-vite vite @vitejs/plugin-react typescript @types/react @types/react-dom @types/node electron-builder vitest jsdom @types/jsdom`.

Runtime-зависимости рендера (Task 2+): `npm i unified remark-parse remark-rehype remark-gfm remark-math rehype-raw rehype-slug rehype-autolink-headings rehype-katex rehype-highlight rehype-sanitize rehype-stringify katex highlight.js github-markdown-css mermaid` (мажоры: unified@11, remark-parse@11, remark-rehype@11, remark-gfm@4, remark-math@6, rehype-*@7/6, katex@0.16, highlight.js@11, github-markdown-css@5, mermaid@11).

- [ ] **Step 2: Конфиги**

`tsconfig.json`:

```json
{
  "files": [],
  "references": [{ "path": "./tsconfig.node.json" }, { "path": "./tsconfig.web.json" }]
}
```

`tsconfig.node.json` (main + preload):

```json
{
  "compilerOptions": {
    "composite": true,
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "bundler",
    "lib": ["ES2022"],
    "strict": true,
    "types": ["node"],
    "skipLibCheck": true
  },
  "include": ["electron.vite.config.ts", "src/main/**/*", "src/preload/**/*", "src/shared/**/*"]
}
```

`tsconfig.web.json` (renderer):

```json
{
  "compilerOptions": {
    "composite": true,
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "bundler",
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "jsx": "react-jsx",
    "strict": true,
    "skipLibCheck": true,
    "baseUrl": ".",
    "paths": { "@shared/*": ["src/shared/*"] }
  },
  "include": ["src/renderer/src/**/*", "src/shared/**/*"]
}
```

`electron.vite.config.ts`:

```ts
import { defineConfig, externalizeDepsPlugin } from 'electron-vite'
import react from '@vitejs/plugin-react'

export default defineConfig({
  main: { plugins: [externalizeDepsPlugin()] },
  preload: { plugins: [externalizeDepsPlugin()] },
  renderer: {
    plugins: [react()],
    resolve: { alias: { '@shared': resolve('src/shared') } }
  }
})
```

(добавь `import { resolve } from 'node:path'` в начало файла)

`.gitignore`:

```
node_modules/
out/
dist/
*.log
```

- [ ] **Step 3: Скелет main-процесса**

`src/main/index.ts`:

```ts
import { app, BrowserWindow } from 'electron'
import { join } from 'node:path'

function createWindow(): void {
  const win = new BrowserWindow({
    width: 1200,
    height: 800,
    title: 'MD Viewer',
    webPreferences: {
      preload: join(__dirname, '../preload/index.js'),
      contextIsolation: true,
      nodeIntegration: false,
      sandbox: true
    }
  })
  if (!app.isPackaged && process.env['ELECTRON_RENDERER_URL']) {
    win.loadURL(process.env['ELECTRON_RENDERER_URL'])
  } else {
    win.loadFile(join(__dirname, '../renderer/index.html'))
  }
}

app.whenReady().then(() => {
  createWindow()
  app.on('activate', () => {
    if (BrowserWindow.getAllWindows().length === 0) createWindow()
  })
})

app.on('window-all-closed', () => {
  if (process.platform !== 'darwin') app.quit()
})
```

- [ ] **Step 4: Скелет preload + renderer**

`src/preload/index.ts`:

```ts
import { contextBridge } from 'electron'

contextBridge.exposeInMainWorld('api', { ping: () => 'pong' })
```

`src/renderer/index.html`:

```html
<!doctype html>
<html lang="ru">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>MD Viewer</title>
  </head>
  <body>
    <div id="root"></div>
    <script type="module" src="/src/main.tsx"></script>
  </body>
</html>
```

`src/renderer/src/main.tsx`:

```tsx
import React from 'react'
import ReactDOM from 'react-dom/client'
import App from './App'
import './styles/global.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
)
```

`src/renderer/src/App.tsx`:

```tsx
export default function App() {
  return <div className="app">MD Viewer</div>
}
```

`src/renderer/src/env.d.ts`:

```ts
/// <reference types="vite/client" />

interface Window {
  api: {
    ping: () => string
  }
}
```

`src/renderer/src/styles/global.css`: базовый сброс (body margin 0, font-family system-ui, min-height 100vh, flex layout по умолчанию).

- [ ] **Step 5: Ручная проверка**

Run: `npm run dev`
Expected: открывается окно «MD Viewer» с надписью MD Viewer; в DevTools консоли `window.api.ping()` → `'pong'`.

- [ ] **Step 6: Commit**

```bash
git add -A
git commit -m "chore: scaffold electron-vite react-ts project"
```

---

### Task 2: Конвейер рендеринга `renderMarkdown` (чистая функция) + тесты

**Files:**

- Create: `src/renderer/src/lib/markdown.ts`
- Test: `tests/markdown.test.ts` (environment: node)
- Modify: `vitest.config.ts` (создать), `package.json` (test scripts уже есть)

**Interfaces:**

- Consumes: —
- Produces: `export async function renderMarkdown(source: string): Promise<string>` — полный HTML-рендер без Mermaid (блоки `language-mermaid` остаются нетронутыми, их заменяет MarkdownView в Task 5).

- [ ] **Step 1: Написать падающий тест**

`vitest.config.ts`:

```ts
import { defineConfig } from 'vitest/config'

export default defineConfig({
  test: {
    environment: 'node',
    include: ['tests/**/*.test.ts']
  }
})
```

`tests/markdown.test.ts`:

```ts
import { describe, it, expect } from 'vitest'
import { renderMarkdown } from '../src/renderer/src/lib/markdown'

describe('renderMarkdown', () => {
  it('рендерит GFM-таблицу', async () => {
    const html = await renderMarkdown('| a | b |\n|---|---|\n| 1 | 2 |')
    expect(html).toContain('<table>')
    expect(html).toContain('<th>')
  })

  it('рендерит task-лист', async () => {
    const html = await renderMarkdown('- [x] done\n- [ ] todo')
    expect(html).toContain('type="checkbox"')
    expect(html).toContain('checked')
  })

  it('рендерит LaTeX через KaTeX', async () => {
    const html = await renderMarkdown('$x^2 + y^2 = z^2$')
    expect(html).toContain('katex')
  })

  it('подсвечивает код через highlight.js', async () => {
    const html = await renderMarkdown('```js\nconst a = 1\n```')
    expect(html).toContain('hljs')
    expect(html).toContain('language-js')
  })

  it('добавляет id заголовкам (для якорей и TOC)', async () => {
    const html = await renderMarkdown('## Привет мир')
    expect(html).toMatch(/<h2 id="привет-мир"/)
  })

  it('вырезает скрипты и обработчики событий', async () => {
    const html = await renderMarkdown('<script>alert(1)</script>\n<img src="x" onerror="alert(2)">')
    expect(html).not.toContain('<script')
    expect(html).not.toContain('onerror')
  })

  it('сохраняет блок mermaid нетронутым', async () => {
    const html = await renderMarkdown('```mermaid\ngraph TD; A-->B;\n```')
    expect(html).toContain('language-mermaid')
    expect(html).toContain('graph TD')
  })

  it('рендерит инлайн-HTML (details/summary)', async () => {
    const html = await renderMarkdown('<details><summary>спойлер</summary>текст</details>')
    expect(html).toContain('<details>')
  })
})
```

- [ ] **Step 2: Запустить и убедиться, что падает**

Run: `npx vitest run tests/markdown.test.ts`
Expected: FAIL — модуль `markdown.ts` не существует.

- [ ] **Step 3: Реализация**

`src/renderer/src/lib/markdown.ts`:

```ts
import { unified } from 'unified'
import remarkParse from 'remark-parse'
import remarkGfm from 'remark-gfm'
import remarkMath from 'remark-math'
import remarkRehype from 'remark-rehype'
import rehypeRaw from 'rehype-raw'
import rehypeSlug from 'rehype-slug'
import rehypeAutolinkHeadings from 'rehype-autolink-headings'
import rehypeKatex from 'rehype-katex'
import rehypeHighlight from 'rehype-highlight'
import rehypeSanitize, { defaultSchema } from 'rehype-sanitize'
import rehypeStringify from 'rehype-stringify'

// defaultSchema (github) + разрешаем className/style везде:
// KaTeX (классы + инлайн-стили), hljs (классы), autolink-headings (класс anchor)
const schema = {
  ...defaultSchema,
  attributes: {
    ...defaultSchema.attributes,
    '*': [...(defaultSchema.attributes['*'] ?? []), 'className', 'style']
  }
}

const processor = unified()
  .use(remarkParse)
  .use(remarkGfm)
  .use(remarkMath)
  .use(remarkRehype, { allowDangerousHtml: true })
  .use(rehypeRaw)
  .use(rehypeSlug)
  .use(rehypeAutolinkHeadings, {
    behavior: 'prepend',
    content: { type: 'text', value: '#' },
    properties: { className: ['anchor'], ariaHidden: 'true' }
  })
  .use(rehypeKatex)
  .use(rehypeHighlight, { detect: false })
  .use(rehypeSanitize, schema)
  .use(rehypeStringify)

export async function renderMarkdown(source: string): Promise<string> {
  const file = await processor.process(source)
  return String(file)
}
```

- [ ] **Step 4: Прогнать тесты**

Run: `npx vitest run tests/markdown.test.ts`
Expected: все 8 PASS. Если тест «id заголовкам» падает (кириллица в slug) — проверь, что `github-slugger` (зависимость rehype-slug) корректно слагифицирует кириллицу; при необходимости включи опцию `maintainCase` или проверь фактический id и поправь ожидание в тесте под фактическое поведение (id обязан существовать и быть стабильным).

- [ ] **Step 5: Commit**

```bash
git add src/renderer/src/lib/markdown.ts tests/markdown.test.ts vitest.config.ts
git commit -m "feat: markdown rendering pipeline (GFM, math, highlight, sanitize)"
```

---

### Task 3: IPC-слой (main + preload) и автооткрытие demo.md

**Files:**

- Modify: `src/main/index.ts`
- Modify: `src/preload/index.ts`
- Modify: `src/renderer/src/env.d.ts`
- Create: `src/shared/types.ts`
- Create: `src/renderer/src/lib/theme.ts` (инъекция CSS тем — нужна App для демо-проверки)
- Test: нет автотестов (ручная проверка через UI)

**Interfaces:**

- Consumes: скелет из Task 1.
- Produces:
  - `src/shared/types.ts`:
    - `interface DirEntry { name: string; path: string; isDir: boolean }`
    - `interface FileContent { content: string; baseDir: string }`
    - `interface PathStat { exists: boolean; isDir: boolean; isFile: boolean }`
  - preload `window.api`:
    - `openFile(): Promise<string | null>`
    - `openFolder(): Promise<string | null>`
    - `readFile(path: string): Promise<FileContent>`
    - `readDir(path: string): Promise<DirEntry[]>`
    - `stat(path: string): Promise<PathStat>`
    - `resolveImage(baseDir: string, src: string): Promise<string | null>`
    - `startFile(): Promise<string | null>` — путь стартового файла (argv `.md` или demo.md в dev)

- [ ] **Step 1: Shared types**

`src/shared/types.ts`:

```ts
export interface DirEntry {
  name: string
  path: string
  isDir: boolean
}

export interface FileContent {
  content: string
  baseDir: string
}

export interface PathStat {
  exists: boolean
  isDir: boolean
  isFile: boolean
}
```

- [ ] **Step 2: IPC в main**

Дополнить `src/main/index.ts`:

```ts
import { app, BrowserWindow, dialog, ipcMain } from 'electron'
import { join, dirname, resolve } from 'node:path'
import { readFile, readdir, stat as fsStat } from 'node:fs/promises'
import { pathToFileURL } from 'node:url'
import type { DirEntry, FileContent, PathStat } from '../shared/types'

let mainWindow: BrowserWindow | null = null
const isDev = !app.isPackaged

function registerIpc(): void {
  ipcMain.handle('dialog:openFile', async () => {
    const res = await dialog.showOpenDialog(mainWindow!, {
      properties: ['openFile'],
      filters: [{ name: 'Markdown', extensions: ['md', 'markdown', 'mdown', 'mkd'] }]
    })
    return res.canceled ? null : res.filePaths[0]
  })

  ipcMain.handle('dialog:openFolder', async () => {
    const res = await dialog.showOpenDialog(mainWindow!, { properties: ['openDirectory'] })
    return res.canceled ? null : res.filePaths[0]
  })

  ipcMain.handle('fs:readFile', async (_e, p: string): Promise<FileContent> => {
    const content = await readFile(p, 'utf8')
    return { content, baseDir: dirname(p) }
  })

  ipcMain.handle('fs:readDir', async (_e, p: string): Promise<DirEntry[]> => {
    const entries = await readdir(p, { withFileTypes: true })
    return entries.map((d) => ({ name: d.name, path: resolve(p, d.name), isDir: d.isDirectory() }))
  })

  ipcMain.handle('fs:stat', async (_e, p: string): Promise<PathStat> => {
    try {
      const s = await fsStat(p)
      return { exists: true, isDir: s.isDirectory(), isFile: s.isFile() }
    } catch {
      return { exists: false, isDir: false, isFile: false }
    }
  })

  ipcMain.handle('img:resolve', async (_e, baseDir: string, src: string): Promise<string | null> => {
    if (/^(https?:|file:|data:)/.test(src)) return null
    const full = resolve(baseDir, decodeURIComponent(src))
    try {
      await fsStat(full)
      return pathToFileURL(full).href
    } catch {
      return null
    }
  })

  ipcMain.handle('app:startFile', async (): Promise<string | null> => {
    const fromArgv = findMdInArgv(process.argv)
    if (fromArgv) return fromArgv
    if (isDev) {
      const demo = join(app.getAppPath(), 'demo.md')
      try {
        await fsStat(demo)
        return demo
      } catch {
        return null
      }
    }
    return null
  })
}

function findMdInArgv(argv: string[]): string | null {
  return argv.find((a) => /\.(md|markdown|mdown|mkd)$/i.test(a) && !a.startsWith('-')) ?? null
}
```

В `createWindow` сохранить ссылку (`mainWindow = win`) и вызвать `registerIpc()` внутри `app.whenReady().then(...)`.

- [ ] **Step 3: Preload**

`src/preload/index.ts`:

```ts
import { contextBridge, ipcRenderer } from 'electron'
import type { DirEntry, FileContent, PathStat } from '../shared/types'

const api = {
  openFile: (): Promise<string | null> => ipcRenderer.invoke('dialog:openFile'),
  openFolder: (): Promise<string | null> => ipcRenderer.invoke('dialog:openFolder'),
  readFile: (path: string): Promise<FileContent> => ipcRenderer.invoke('fs:readFile', path),
  readDir: (path: string): Promise<DirEntry[]> => ipcRenderer.invoke('fs:readDir', path),
  stat: (path: string): Promise<PathStat> => ipcRenderer.invoke('fs:stat', path),
  resolveImage: (baseDir: string, src: string): Promise<string | null> =>
    ipcRenderer.invoke('img:resolve', baseDir, src),
  startFile: (): Promise<string | null> => ipcRenderer.invoke('app:startFile')
}

contextBridge.exposeInMainWorld('api', api)
export type Api = typeof api
```

- [ ] **Step 4: Типы в renderer**

`src/renderer/src/env.d.ts` заменить на:

```ts
/// <reference types="vite/client" />
import type { Api } from '../../preload/index'

declare global {
  interface Window {
    api: Api
  }
}
export {}
```

- [ ] **Step 5: App грузит стартовый файл**

`src/renderer/src/App.tsx`:

```tsx
import { useEffect, useState } from 'react'

export default function App() {
  const [source, setSource] = useState<string>('')
  const [baseDir, setBaseDir] = useState<string>('')
  const [filePath, setFilePath] = useState<string>('')
  const [status, setStatus] = useState<string>('')

  async function loadFile(path: string): Promise<void> {
    try {
      const { content, baseDir } = await window.api.readFile(path)
      setSource(content)
      setBaseDir(baseDir)
      setFilePath(path)
      setStatus(path)
    } catch (err) {
      setStatus(`Ошибка чтения: ${String(err)}`)
    }
  }

  useEffect(() => {
    window.api.startFile().then((p) => p && loadFile(p))
  }, [])

  return (
    <div className="app">
      <pre className="raw-view">{source || 'Загрузите .md файл'}</pre>
      <div className="status">{status}</div>
    </div>
  )
}
```

- [ ] **Step 6: demo.md + ручная проверка**

Создать `demo.md` (заголовки, таблица, task-лист, код, формула `$E=mc^2$`, блок mermaid, картинка с относительным путём — временно без неё).

Run: `npm run dev`
Expected: в окне отображается исходник demo.md и путь в статус-баре. `window.api.stat('C:\\')` в DevTools → `{ exists: true, isDir: true, ... }`.

- [ ] **Step 7: Commit**

```bash
git add src/main src/preload src/shared src/renderer/src/env.d.ts src/renderer/src/App.tsx demo.md
git commit -m "feat: ipc bridge and demo file auto-load"
```

---

### Task 4: Тема + MarkdownView (рендер в стиле GitHub) + переключатель темы

**Files:**

- Create: `src/renderer/src/lib/theme.ts`
- Create: `src/renderer/src/components/MarkdownView.tsx`
- Modify: `src/renderer/src/App.tsx`
- Modify: `src/renderer/src/styles/global.css`
- Test: нет автотестов (ручная проверка: demo.md рендерится в обеих темах)

**Interfaces:**

- Consumes: `renderMarkdown` (Task 2), `window.api.resolveImage` (Task 3).
- Produces:
  - `lib/theme.ts`: `applyTheme(dark: boolean): void` — инъекция `<style id="theme-css">` с github-markdown-{light,dark}.css + highlight.js github{,-dark}.css (`?inline` строки); переключает класс `dark` на `document.documentElement`.
  - `components/MarkdownView.tsx`: props `{ html: string; baseDir: string; dark: boolean; onRendered?: (el: HTMLElement) => void }` — вставляет `html` в `.markdown-body`, резолвит картинки, зовёт `onRendered` после пост-обработки.

- [ ] **Step 1: theme.ts**

```ts
import markdownLight from 'github-markdown-css/github-markdown-light.css?inline'
import markdownDark from 'github-markdown-css/github-markdown-dark.css?inline'
import hljsLight from 'highlight.js/styles/github.css?inline'
import hljsDark from 'highlight.js/styles/github-dark.css?inline'

let styleEl: HTMLStyleElement | null = null

export function applyTheme(dark: boolean): void {
  if (!styleEl) {
    styleEl = document.createElement('style')
    styleEl.id = 'theme-css'
    document.head.appendChild(styleEl)
  }
  styleEl.textContent = dark ? `${markdownDark}\n${hljsDark}` : `${markdownLight}\n${hljsLight}`
  document.documentElement.classList.toggle('dark', dark)
}
```

Примечание: если subpath-импорт css из node_modules блокируется exports-полем пакета — скопировать 4 css-файла в `src/renderer/src/assets/` и импортировать оттуда (Vite обработает `?inline` в любом случае).

- [ ] **Step 2: resolveImages + MarkdownView**

`src/renderer/src/lib/resolveImages.ts`:

```ts
export async function resolveImageSrcs(
  container: HTMLElement,
  baseDir: string,
  resolve: (baseDir: string, src: string) => Promise<string | null>
): Promise<void> {
  const imgs = Array.from(container.querySelectorAll<HTMLImageElement>('img'))
  for (const img of imgs) {
    const src = img.getAttribute('src')
    if (!src) continue
    if (/^(https?:|file:|data:)/.test(src) || src.startsWith('#')) continue
    const resolved = await resolve(baseDir, src)
    if (resolved) img.setAttribute('src', resolved)
  }
}
```

`src/renderer/src/components/MarkdownView.tsx`:

```tsx
import { useEffect, useRef } from 'react'
import { resolveImageSrcs } from '../lib/resolveImages'

interface Props {
  html: string
  baseDir: string
  onRendered?: (el: HTMLElement) => void
}

export default function MarkdownView({ html, baseDir, onRendered }: Props) {
  const ref = useRef<HTMLDivElement>(null)
  const onRenderedRef = useRef(onRendered)
  onRenderedRef.current = onRendered

  useEffect(() => {
    let cancelled = false
    const el = ref.current
    if (!el) return
    el.innerHTML = html
    resolveImageSrcs(el, baseDir, window.api.resolveImage).then(() => {
      if (!cancelled) onRenderedRef.current?.(el)
    })
    return () => {
      cancelled = true
    }
  }, [html, baseDir])

  return <div className="markdown-body" ref={ref} />
}
```

- [ ] **Step 3: App — рендер, тема, кнопка переключения**

`src/renderer/src/App.tsx`:

```tsx
import { useEffect, useState } from 'react'
import { renderMarkdown } from './lib/markdown'
import { applyTheme } from './lib/theme'
import MarkdownView from './components/MarkdownView'

export default function App() {
  const [source, setSource] = useState('')
  const [baseDir, setBaseDir] = useState('')
  const [filePath, setFilePath] = useState('')
  const [status, setStatus] = useState('')
  const [dark, setDark] = useState(() => localStorage.getItem('theme') === 'dark')

  const [renderedHtml, setRenderedHtml] = useState('')

  async function loadFile(path: string): Promise<void> {
    try {
      const { content, baseDir } = await window.api.readFile(path)
      setSource(content)
      setBaseDir(baseDir)
      setFilePath(path)
      setStatus(path)
    } catch (err) {
      setStatus(`Ошибка чтения: ${String(err)}`)
    }
  }

  useEffect(() => {
    applyTheme(dark)
    localStorage.setItem('theme', dark ? 'dark' : 'light')
  }, [dark])

  useEffect(() => {
    let cancelled = false
    renderMarkdown(source).then((h) => {
      if (!cancelled) setRenderedHtml(h)
    })
    return () => {
      cancelled = true
    }
  }, [source])

  useEffect(() => {
    window.api.startFile().then((p) => p && loadFile(p))
  }, [])

  return (
    <div className="app">
      <header className="toolbar">
        <span className="path">{filePath}</span>
        <button onClick={() => setDark((d) => !d)}>{dark ? '☀️ Светлая' : '🌙 Тёмная'}</button>
      </header>
      <main className="content">
        <MarkdownView html={renderedHtml} baseDir={baseDir} />
      </main>
      <footer className="status">{status}</footer>
    </div>
  )
}
```

Рендер асинхронный: `source` → эффект → `setRenderedHtml` → MarkdownView.

`global.css` — раскладка:

```css
:root {
  font-family: system-ui, -apple-system, 'Segoe UI', sans-serif;
}
* { box-sizing: border-box; }
body { margin: 0; background: #ffffff; }
html.dark body { background: #0d1117; }
.app { display: flex; flex-direction: column; height: 100vh; }
.toolbar {
  display: flex; align-items: center; gap: 8px;
  padding: 6px 12px; border-bottom: 1px solid #d0d7de;
  font-size: 13px;
}
html.dark .toolbar { border-color: #30363d; color: #e6edf3; }
.path { flex: 1; overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
.content { flex: 1; overflow: auto; padding: 24px; }
.status { padding: 4px 12px; font-size: 12px; color: #57606a; border-top: 1px solid #d0d7de; }
html.dark .status { color: #8b949e; border-color: #30363d; }
```

- [ ] **Step 4: Ручная проверка**

Run: `npm run dev`
Expected: demo.md рендерится со стилями GitHub (таблицы, код с подсветкой, формулы KaTeX); кнопка переключает тёмную/светлую тему; `.markdown-body` в DevTools имеет `data-theme` не нужен — тема через класс на `<html>`.

- [ ] **Step 5: Commit**

```bash
git add src/renderer
git commit -m "feat: github-style rendering with light/dark theme"
```

---

### Task 5: Mermaid + TOC-сайдбар

**Files:**

- Create: `src/renderer/src/lib/mermaid.ts`
- Create: `src/renderer/src/lib/toc.ts`
- Create: `src/renderer/src/components/TocSidebar.tsx`
- Modify: `src/renderer/src/components/MarkdownView.tsx` (вызов `renderMermaid` + колбэк TOC)
- Modify: `src/renderer/src/App.tsx` (сайдбар, кнопка «Оглавление»)
- Test: `tests/toc.test.ts` (environment: jsdom)

**Interfaces:**

- Consumes: `renderMarkdown` (Task 2), MarkdownView (Task 4).
- Produces:
  - `lib/mermaid.ts`: `renderMermaid(container: HTMLElement, dark: boolean): Promise<void>` — заменяет `pre > code.language-mermaid` на svg; при ошибке — div с классом `mermaid-error` и текстом ошибки.
  - `lib/toc.ts`: `interface TocEntry { id: string; text: string; level: number }`; `extractToc(container: HTMLElement, maxLevel?: number): TocEntry[]` (по умолчанию 4).
  - `components/TocSidebar.tsx`: props `{ entries: TocEntry[]; onNavigate: (id: string) => void }`.

- [ ] **Step 1: Падающий тест TOC**

В `vitest.config.ts` добавить проекты окружений: используй `// @vitest-environment jsdom` комментарий в начале файла теста (проще, чем менять конфиг).

`tests/toc.test.ts`:

```ts
// @vitest-environment jsdom
import { describe, it, expect } from 'vitest'
import { extractToc } from '../src/renderer/src/lib/toc'

function container(html: string): HTMLElement {
  const div = document.createElement('div')
  div.innerHTML = html
  return div
}

describe('extractToc', () => {
  it('собирает заголовки с id, текстом и уровнем', () => {
    const el = container('<h1 id="a">Заголовок</h1><h2 id="b">Подзаголовок</h2><p>текст</p>')
    expect(extractToc(el)).toEqual([
      { id: 'a', text: 'Заголовок', level: 1 },
      { id: 'b', text: 'Подзаголовок', level: 2 }
    ])
  })

  it('исключает текст якоря (#) из заголовка', () => {
    const el = container('<h2 id="c"><a class="anchor" aria-hidden="true">#</a>Привет</h2>')
    expect(extractToc(el)[0].text).toBe('Привет')
  })

  it('ограничивает уровень maxLevel', () => {
    const el = container('<h3 id="h3">A</h3><h5 id="h5">B</h5>')
    expect(extractToc(el, 4).map((e) => e.id)).toEqual(['h3'])
  })
})
```

- [ ] **Step 2: Запустить — убедиться, что падает**

Run: `npx vitest run tests/toc.test.ts`
Expected: FAIL — `toc.ts` не существует.

- [ ] **Step 3: Реализация toc.ts + mermaid.ts**

`src/renderer/src/lib/toc.ts`:

```ts
export interface TocEntry {
  id: string
  text: string
  level: number
}

export function extractToc(container: HTMLElement, maxLevel = 4): TocEntry[] {
  const levels = Array.from({ length: maxLevel }, (_, i) => `h${i + 1}`).join(',')
  const heads = Array.from(container.querySelectorAll<HTMLElement>(levels))
  return heads
    .filter((h) => h.id)
    .map((h) => ({
      id: h.id,
      text: Array.from(h.childNodes)
        .filter((n) => !(n instanceof HTMLElement && n.classList.contains('anchor')))
        .map((n) => n.textContent ?? '')
        .join('')
        .trim(),
      level: Number(h.tagName[1])
    }))
}
```

`src/renderer/src/lib/mermaid.ts`:

```ts
export async function renderMermaid(container: HTMLElement, dark: boolean): Promise<void> {
  const blocks = Array.from(container.querySelectorAll<HTMLElement>('pre > code.language-mermaid'))
  if (blocks.length === 0) return

  const mermaid = (await import('mermaid')).default
  mermaid.initialize({
    startOnLoad: false,
    theme: dark ? 'dark' : 'default',
    securityLevel: 'strict',
    fontFamily: 'system-ui, sans-serif'
  })

  let i = 0
  for (const code of blocks) {
    const pre = code.parentElement
    if (!pre) continue
    const raw = code.textContent ?? ''
    const id = `mmd-${++i}-${Date.now()}`
    try {
      const { svg } = await mermaid.render(id, raw)
      const holder = document.createElement('div')
      holder.className = 'mermaid'
      holder.innerHTML = svg
      pre.replaceWith(holder)
    } catch (err) {
      const errEl = document.createElement('div')
      errEl.className = 'mermaid-error'
      errEl.textContent = `Ошибка Mermaid: ${err instanceof Error ? err.message : String(err)}`
      pre.replaceWith(errEl)
    }
  }
}
```

- [ ] **Step 4: Подключить в MarkdownView**

`src/renderer/src/components/MarkdownView.tsx` — дополнить:

```tsx
import { renderMermaid } from '../lib/mermaid'
import { extractToc, type TocEntry } from '../lib/toc'

interface Props {
  html: string
  baseDir: string
  dark: boolean
  onRendered?: (el: HTMLElement) => void
  onToc?: (entries: TocEntry[]) => void
}
```

Внутри `useEffect` после `resolveImageSrcs`:

```ts
resolveImageSrcs(el, baseDir, window.api.resolveImage)
  .then(async () => {
    await renderMermaid(el, dark)
    if (!cancelled) {
      onRenderedRef.current?.(el)
      onTocRef.current?.(extractToc(el))
    }
  })
```

(добавь `onTocRef` по аналогии с `onRenderedRef`; добавь `dark` в deps эффекта).

- [ ] **Step 5: TocSidebar**

`src/renderer/src/components/TocSidebar.tsx`:

```tsx
import type { TocEntry } from '../lib/toc'

interface Props {
  entries: TocEntry[]
  onNavigate: (id: string) => void
}

export default function TocSidebar({ entries, onNavigate }: Props) {
  if (entries.length === 0) return <aside className="toc"><span className="toc-empty">Нет заголовков</span></aside>
  return (
    <aside className="toc">
      <div className="toc-title">Оглавление</div>
      <nav>
        {entries.map((e) => (
          <a
            key={e.id}
            href={`#${e.id}`}
            className="toc-link"
            style={{ paddingLeft: `${(e.level - 1) * 12}px` }}
            onClick={(ev) => {
              ev.preventDefault()
              onNavigate(e.id)
            }}
          >
            {e.text}
          </a>
        ))}
      </nav>
    </aside>
  )
}
```

- [ ] **Step 6: App — сайдбар, кнопка, скролл к якорю**

`App.tsx` — добавить состояние `const [toc, setToc] = useState<TocEntry[]>([])` и `const [tocOpen, setTocOpen] = useState(true)`; в `onToc` передать `setToc`; функция:

```tsx
function scrollToId(id: string): void {
  document.getElementById(id)?.scrollIntoView({ behavior: 'smooth', block: 'start' })
}
```

Разметка main:

```tsx
<main className="content">
  <div className="content-inner">
    <MarkdownView html={renderedHtml} baseDir={baseDir} dark={dark} onToc={setToc} />
    {tocOpen && <TocSidebar entries={toc} onNavigate={scrollToId} />}
  </div>
</main>
```

CSS (global.css): `.content-inner { display: flex; gap: 24px; max-width: 1100px; margin: 0 auto; }`, `.toc { position: sticky; top: 0; align-self: flex-start; width: 220px; max-height: calc(100vh - 80px); overflow: auto; font-size: 13px; }`, `.toc-link { display: block; color: #57606a; text-decoration: none; padding: 3px 8px; border-radius: 4px; }` (+ hover; тёмная тема — цвета из палитры GitHub `#8b949e`).

- [ ] **Step 7: Прогнать тесты + ручная проверка**

Run: `npx vitest run` — все тесты PASS.
Run: `npm run dev` — в demo.md Mermaid-диаграмма рендерится, TOC-сайдбар показывает заголовки, клик скроллит; кнопка «Оглавление» скрывает/показывает сайдбар.

- [ ] **Step 8: Commit**

```bash
git add src/renderer tests/toc.test.ts
git commit -m "feat: mermaid diagrams and table of contents sidebar"
```

---

### Task 6: Дерево файлов + кнопки «Открыть файл/папку» + drag&drop

**Files:**

- Create: `src/renderer/src/components/FileTree.tsx`
- Modify: `src/renderer/src/App.tsx` (кнопки, drag&drop, ленивое раскрытие)
- Test: нет автотестов (ручная проверка)

**Interfaces:**

- Consumes: `window.api.openFile/openFolder/readDir/stat` (Task 3).
- Produces:
  - `components/FileTree.tsx`: props `{ root: string; onOpenFile: (path: string) => void }` — дерево с ленивым раскрытием папок; `.md`-файлы кликабельны.
  - App: `handleOpenFile()` / `handleOpenFolder()` (диалоги), `handleDrop(e: DragEvent)`.

- [ ] **Step 1: FileTree**

`src/renderer/src/components/FileTree.tsx`:

```tsx
import { useState } from 'react'
import type { DirEntry } from '@shared/types'

interface Props {
  root: string
  onOpenFile: (path: string) => void
}

interface NodeState {
  entry: DirEntry
  expanded: boolean
  children: DirEntry[] | null
  loading: boolean
}

function TreeNode({
  node,
  depth,
  onOpenFile
}: {
  node: NodeState
  depth: number
  onOpenFile: (path: string) => void
}) {
  const [state, setState] = useState(node)
  const isMd = /\.(md|markdown|mdown|mkd)$/i.test(node.entry.name)

  async function toggle(): Promise<void> {
    if (!node.entry.isDir) {
      onOpenFile(node.entry.path)
      return
    }
    if (state.children === null) {
      setState((s) => ({ ...s, loading: true }))
      const children = await window.api.readDir(node.entry.path)
      setState((s) => ({ ...s, children, loading: false, expanded: true }))
    } else {
      setState((s) => ({ ...s, expanded: !s.expanded }))
    }
  }

  return (
    <div>
      <div
        className={`tree-row${node.entry.isDir ? ' dir' : ''}${isMd ? ' md' : ''}`}
        style={{ paddingLeft: `${depth * 14}px` }}
        onClick={toggle}
        title={node.entry.path}
      >
        <span className="tree-icon">{node.entry.isDir ? (state.expanded ? '📂' : '📁') : '📄'}</span>
        <span className="tree-name">{node.entry.name}</span>
        {state.loading && <span className="tree-loading">…</span>}
      </div>
      {node.entry.isDir && state.expanded && state.children && (
        <div>
          {state.children.map((c) => (
            <TreeNode key={c.path} node={{ entry: c, expanded: false, children: null, loading: false }} depth={depth + 1} onOpenFile={onOpenFile} />
          ))}
        </div>
      )}
    </div>
  )
}

export default function FileTree({ root, onOpenFile }: Props) {
  const [rootEntry] = useState<DirEntry>({ name: root.split(/[\\/]/).pop() ?? root, path: root, isDir: true })
  return (
    <aside className="tree">
      <div className="tree-root" title={root}>{rootEntry.name}</div>
      <div className="tree-scroll">
        <TreeNode node={{ entry: rootEntry, expanded: true, children: null, loading: false }} depth={0} onOpenFile={onOpenFile} />
      </div>
    </aside>
  )
}
```

Примечание: ленивая загрузка реализована через локальное состояние внутри TreeNode (дети загружаются по клику). Это нормально для нашего объёма; перечитывание папки — по кнопке «Открыть папку» заново.

- [ ] **Step 2: App — кнопки, root, drag&drop**

`App.tsx` — добавить:

```tsx
const [root, setRoot] = useState<string | null>(null)

async function handleOpenFile(): Promise<void> {
  const p = await window.api.openFile()
  if (p) await loadFile(p)
}

async function handleOpenFolder(): Promise<void> {
  const p = await window.api.openFolder()
  if (p) {
    setRoot(p)
    setStatus(p)
  }
}

async function handleDrop(e: React.DragEvent): Promise<void> {
  e.preventDefault()
  const file = e.dataTransfer.files[0]
  if (!file) return
  const path = (file as File & { path?: string }).path
  if (!path) return
  const st = await window.api.stat(path)
  if (st.isDir) {
    setRoot(path)
    setStatus(path)
  } else if (st.isFile) {
    await loadFile(path)
  }
}
```

В `useEffect` автозагрузки: если `startFile()` вернул файл — `loadFile(p)`, если это dev и demo.md — путь demo.md, ок.

Toolbar:

```tsx
<header className="toolbar">
  <button onClick={handleOpenFile}>📂 Открыть файл</button>
  <button onClick={handleOpenFolder}>🗀 Папка</button>
  <span className="path">{filePath}</span>
  <button onClick={() => setTocOpen((v) => !v)}>Оглавление</button>
  <button onClick={() => setDark((d) => !d)}>{dark ? '☀️' : '🌙'}</button>
</header>
```

На корневой div App — `onDragOver={(e) => e.preventDefault()}` и `onDrop={handleDrop}`.

Main:

```tsx
<main className="content">
  {root && <FileTree root={root} onOpenFile={loadFile} />}
  <div className="content-inner"> ... MarkdownView + TocSidebar ... </div>
</main>
```

CSS: `.content { display: flex; gap: 16px; }`, `.tree { width: 240px; flex-shrink: 0; border-right: 1px solid #d0d7de; overflow: auto; font-size: 13px; }`, `.tree-row { padding: 3px 8px; cursor: pointer; white-space: nowrap; display: flex; gap: 6px; align-items: center; }`, `.tree-row:hover { background: rgba(175,184,193,0.2); }`, `.tree-row.md .tree-name { color: #0969da; }` (тёмная: `#58a6ff`).

- [ ] **Step 3: Ручная проверка**

Run: `npm run dev`
Expected: кнопки открывают диалоги; «Открыть папку» строит дерево; клик по `.md` в дереве рендерит файл (картинки резолвятся от его папки); перетаскивание `.md` или папки на окно работает.

- [ ] **Step 4: Commit**

```bash
git add src/renderer
git commit -m "feat: file tree, open file/folder dialogs, drag and drop"
```

---

### Task 7: Поиск с подсветкой

**Files:**

- Create: `src/renderer/src/lib/search.ts`
- Create: `src/renderer/src/components/SearchBar.tsx`
- Modify: `src/renderer/src/App.tsx` (состояние поиска, колбэк подсветки)
- Modify: `src/renderer/src/components/MarkdownView.tsx` (применение поиска после рендера)
- Test: `tests/search.test.ts` (environment: jsdom)

**Interfaces:**

- Consumes: MarkdownView (Task 4).
- Produces:
  - `lib/search.ts`:
    - `highlightMatches(container: HTMLElement, query: string): number` — убирает старые `<mark>`, подсвечивает все совпадения (регистронезависимо), возвращает счётчик.
    - `clearHighlights(container: HTMLElement): void`
  - `components/SearchBar.tsx`: props `{ query: string; onChange: (q: string) => void; count: number }`.

- [ ] **Step 1: Падающий тест**

`tests/search.test.ts`:

```ts
// @vitest-environment jsdom
import { describe, it, expect } from 'vitest'
import { highlightMatches, clearHighlights } from '../src/renderer/src/lib/search'

function container(html: string): HTMLElement {
  const div = document.createElement('div')
  div.innerHTML = html
  return div
}

describe('highlightMatches', () => {
  it('подсвечивает совпадения и возвращает их количество', () => {
    const el = container('<p>foo bar foo</p>')
    expect(highlightMatches(el, 'foo')).toBe(2)
    expect(el.querySelectorAll('mark').length).toBe(2)
  })

  it('регистронезависим', () => {
    const el = container('<p>Hello HELLO hello</p>')
    expect(highlightMatches(el, 'hello')).toBe(3)
  })

  it('повторный вызов сбрасывает старые mark', () => {
    const el = container('<p>foo foo</p>')
    highlightMatches(el, 'foo')
    expect(highlightMatches(el, 'zzz')).toBe(0)
    expect(el.querySelectorAll('mark').length).toBe(0)
    expect(el.textContent).toBe('foo foo')
  })

  it('пустой запрос — 0 совпадений', () => {
    const el = container('<p>foo</p>')
    expect(highlightMatches(el, '')).toBe(0)
  })

  it('не трогает код и стили', () => {
    const el = container('<p>a b</p><script>foo</script><style>.foo{}</style>')
    expect(highlightMatches(el, 'foo')).toBe(0)
  })
})
```

- [ ] **Step 2: Запустить — убедиться, что падает**

Run: `npx vitest run tests/search.test.ts`
Expected: FAIL.

- [ ] **Step 3: Реализация search.ts**

`src/renderer/src/lib/search.ts`:

```ts
function escapeRegExp(s: string): string {
  return s.replace(/[.*+?^${}()|[\]\\]/g, '\\$&')
}

export function clearHighlights(container: HTMLElement): void {
  for (const mark of Array.from(container.querySelectorAll('mark'))) {
    const parent = mark.parentNode
    if (!parent) continue
    parent.replaceChild(document.createTextNode(mark.textContent ?? ''), mark)
    parent.normalize()
  }
}

export function highlightMatches(container: HTMLElement, query: string): number {
  clearHighlights(container)
  const q = query.trim()
  if (!q) return 0

  const re = new RegExp(escapeRegExp(q), 'gi')
  const textNodes: Text[] = []
  const walker = document.createTreeWalker(container, NodeFilter.SHOW_TEXT, {
    acceptNode(node) {
      const p = (node as Text).parentElement
      if (p && (p.tagName === 'SCRIPT' || p.tagName === 'STYLE' || p.tagName === 'MARK')) {
        return NodeFilter.FILTER_REJECT
      }
      return NodeFilter.FILTER_ACCEPT
    }
  })
  while (walker.nextNode()) textNodes.push(walker.currentNode as Text)

  let count = 0
  for (const node of textNodes) {
    const text = node.nodeValue ?? ''
    if (!re.test(text)) continue
    re.lastIndex = 0
    const frag = document.createDocumentFragment()
    let last = 0
    let m: RegExpExecArray | null
    while ((m = re.exec(text)) !== null) {
      if (m.index > last) frag.appendChild(document.createTextNode(text.slice(last, m.index)))
      const mark = document.createElement('mark')
      mark.textContent = m[0]
      frag.appendChild(mark)
      count++
      last = m.index + m[0].length
    }
    if (last < text.length) frag.appendChild(document.createTextNode(text.slice(last)))
    node.replaceWith(frag)
  }
  return count
}
```

- [ ] **Step 4: SearchBar**

`src/renderer/src/components/SearchBar.tsx`:

```tsx
interface Props {
  query: string
  onChange: (q: string) => void
  count: number
}

export default function SearchBar({ query, onChange, count }: Props) {
  return (
    <div className="search">
      <input
        type="search"
        placeholder="Поиск по документу…"
        value={query}
        onChange={(e) => onChange(e.target.value)}
        aria-label="Поиск по документу"
      />
      {query.trim() !== '' && <span className="search-count">{count}</span>}
    </div>
  )
}
```

- [ ] **Step 5: Подключить в MarkdownView + App**

`MarkdownView.tsx` — пропсы + применение:

```tsx
import { highlightMatches } from '../lib/search'

interface Props {
  html: string
  baseDir: string
  dark: boolean
  query: string
  onRendered?: (el: HTMLElement) => void
  onToc?: (entries: TocEntry[]) => void
  onSearchCount?: (n: number) => void
}
```

В эффекте после `extractToc`:

```ts
const count = highlightMatches(el, query)
onSearchCountRef.current?.(count)
```

(добавь `onSearchCountRef`; добавь `query` в deps.)

`App.tsx` — состояние `const [query, setQuery] = useState('')` и `const [searchCount, setSearchCount] = useState(0)`; `<SearchBar query={query} onChange={setQuery} count={searchCount} />` в toolbar; передать `query` и `onSearchCount={setSearchCount}` в MarkdownView.

CSS: `.search input { padding: 4px 8px; border: 1px solid #d0d7de; border-radius: 6px; font-size: 13px; }`, `mark { background: #fff8c5; color: inherit; padding: 0; }` (тёмная: `#2ea04366`? нет — GitHub-стиль: `mark { background-color: #fff8c5; }`, тёмная `#d29922` как фоновый текст... используй `html.dark mark { background: #6e40c9aa; }`? Проще: `html.dark mark { background: #9e6a03; color: #fff; }`), `.search-count { font-size: 12px; color: #57606a; }`.

- [ ] **Step 6: Тесты + ручная проверка**

Run: `npx vitest run` — все PASS.
Run: `npm run dev` — ввод в поиске подсвечивает совпадения, счётчик обновляется, повторный ввод сбрасывает старые mark.

- [ ] **Step 7: Commit**

```bash
git add src/renderer tests/search.test.ts
git commit -m "feat: in-document search with highlighting"
```

---

### Task 8: Переключатель «Рендер/Исходник» + статус-бар ошибок + клавиатура

**Files:**

- Create: `src/renderer/src/components/SourceView.tsx`
- Modify: `src/renderer/src/App.tsx`
- Modify: `src/renderer/src/styles/global.css`
- Test: нет автотестов (ручная проверка)

**Interfaces:**

- Consumes: App state (Task 3–7).
- Produces: `mode: 'render' | 'source'` в App; `SourceView` с сырым текстом.

- [ ] **Step 1: SourceView**

`src/renderer/src/components/SourceView.tsx`:

```tsx
interface Props {
  source: string
}

export default function SourceView({ source }: Props) {
  return (
    <pre className="source-view">
      <code>{source}</code>
    </pre>
  )
}
```

- [ ] **Step 2: App — режим, хоткеи**

`App.tsx`:

```tsx
const [mode, setMode] = useState<'render' | 'source'>('render')

useEffect(() => {
  function onKey(e: KeyboardEvent): void {
    if ((e.ctrlKey || e.metaKey) && e.key.toLowerCase() === 'o') {
      e.preventDefault()
      void handleOpenFile()
    }
    if ((e.ctrlKey || e.metaKey) && e.shiftKey && e.key.toLowerCase() === 'o') {
      e.preventDefault()
      void handleOpenFolder()
    }
  }
  window.addEventListener('keydown', onKey)
  return () => window.removeEventListener('keydown', onKey)
}, [])
```

Кнопка в toolbar: `<button onClick={() => setMode((m) => (m === 'render' ? 'source' : 'render'))}>{mode === 'render' ? 'Исходник' : 'Рендер'}</button>`.

В main:

```tsx
{mode === 'render' ? (
  <MarkdownView ... />
) : (
  <SourceView source={source} />
)}
```

- [ ] **Step 3: CSS + ручная проверка**

`global.css`: `.source-view { margin: 0; padding: 16px; background: #f6f8fa; border: 1px solid #d0d7de; border-radius: 8px; overflow: auto; font-family: ui-monospace, SFMono-Regular, Consolas, monospace; font-size: 13px; line-height: 1.5; white-space: pre; }` (+ тёмная: `#161b22` / `#30363d`).

Run: `npm run dev`
Expected: Ctrl+O открывает диалог; переключатель рендер/исходник работает; ошибка чтения (открыть недоступный файл через dialog, выбрать произвольный битый путь) показывает сообщение в статус-баре без краха.

- [ ] **Step 4: Commit**

```bash
git add src/renderer
git commit -m "feat: render/source toggle, keyboard shortcuts, status bar"
```

---

### Task 9: electron-builder + ассоциация .md + скрипты реестра

**Files:**

- Modify: `package.json` (поле `build`)
- Create: `scripts/register-assoc.mjs`
- Create: `scripts/unregister-assoc.mjs`
- Modify: `src/main/index.ts` (single instance + обработка argv)
- Test: сборка `npm run build:win`; проверка ассоциации

**Interfaces:**

- Consumes: Task 1–8.
- Produces: `dist/MD Viewer Setup 0.1.0.exe` (NSIS); dev-ассоциация через `npm run register-assoc`.

- [ ] **Step 1: single instance + открытие файла из argv**

В начало `src/main/index.ts` (до `app.whenReady`):

```ts
if (!app.requestSingleInstanceLock()) {
  app.quit()
} else {
  app.on('second-instance', (_e, argv) => {
    const f = findMdInArgv(argv)
    if (mainWindow) {
      if (mainWindow.isMinimized()) mainWindow.restore()
      mainWindow.focus()
      if (f) mainWindow.webContents.send('app:open-path', f)
    }
  })
}
```

В renderer App — слушать это событие. Добавить в preload:

```ts
onOpenPath: (cb: (path: string) => void): (() => void) => {
  const listener = (_e: Electron.IpcRendererEvent, p: string): void => cb(p)
  ipcRenderer.on('app:open-path', listener)
  return () => ipcRenderer.removeListener('app:open-path', listener)
}
```

App:

```tsx
useEffect(() => {
  return window.api.onOpenPath((p) => void loadFile(p))
}, [])
```

- [ ] **Step 2: Скрипты реестра (dev-ассоциация)**

`scripts/register-assoc.mjs`:

```js
import { spawnSync } from 'node:child_process'
import { fileURLToPath } from 'node:url'
import { dirname, join } from 'node:path'

const root = dirname(dirname(fileURLToPath(import.meta.url)))
const electronExe = join(root, 'node_modules', 'electron', 'dist', 'electron.exe')
const mainJs = join(root, 'out', 'main', 'index.js')
const command = `"${electronExe}" "${mainJs}" "%1"`
const progId = 'MDViewer.md'

function reg(args) {
  const r = spawnSync('reg', args, { encoding: 'utf8' })
  if (r.status !== 0) {
    console.error('reg failed:', args.join(' '), r.stderr)
    process.exitCode = 1
  }
}

reg(['add', 'HKCU\\Software\\Classes\\.md', '/ve', '/d', progId, '/f'])
reg(['add', `HKCU\\Software\\Classes\\${progId}`, '/ve', '/d', 'Markdown Document', '/f'])
reg(['add', `HKCU\\Software\\Classes\\${progId}\\DefaultIcon`, '/ve', '/d', `"${electronExe},0"`, '/f'])
reg(['add', `HKCU\\Software\\Classes\\${progId}\\shell\\open\\command`, '/ve', '/d', command, '/f'])
console.log('OK: .md теперь открывается через dev-сборку. Запустите: npm run dev (или соберите out/main)')
```

`scripts/unregister-assoc.mjs`:

```js
import { spawnSync } from 'node:child_process'

for (const key of ['HKCU\\Software\\Classes\\MDViewer.md', 'HKCU\\Software\\Classes\\.md']) {
  spawnSync('reg', ['delete', key, '/f'], { encoding: 'utf8' })
}
console.log('OK: ассоциация удалена')
```

Примечание: `%1` в аргументе — передаётся как есть (никакой shell-подстановки нет, т.к. используется `spawnSync` без shell).

- [ ] **Step 3: electron-builder**

В `package.json` добавить:

```json
"build": {
  "appId": "com.mdviewer.app",
  "productName": "MD Viewer",
  "directories": { "output": "dist" },
  "files": ["out/**"],
  "win": {
    "target": ["nsis"],
    "fileAssociations": [
      { "ext": "md", "name": "Markdown Document", "description": "Markdown файл", "role": "Editor" }
    ]
  },
  "nsis": { "oneClick": false, "allowToChangeInstallationDirectory": true }
}
```

- [ ] **Step 4: Сборка**

Run: `npm run build:win`
Expected: `dist/MD Viewer Setup 0.1.0.exe` собран без ошибок. (electron-builder качает electron dist — нужен интернет; при ошибке подписи/кода — не критично, NSIS без подписи соберётся.)

- [ ] **Step 5: Проверка ассоциации в dev**

Run: `npm run register-assoc`, затем `npm run dev` (должен быть собран `out/main/index.js` — он есть после `npm run dev`/`build`), двойной клик по любому `.md` в проводнике → окно с этим файлом. При уже запущенном приложении — фокусируется и открывает файл. Затем `npm run unregister-assoc`.

- [ ] **Step 6: Commit**

```bash
git add package.json scripts
git commit -m "feat: electron-builder config and windows .md file association"
```

---

### Task 10: demo.md (полный) + README + финальная проверка по критериям

**Files:**

- Create: `demo.md` (заполнить полностью)
- Create: `README.md`
- Modify: `.gitignore` (при необходимости)
- Test: `npm run test` + полный ручной прогон чек-листа

- [ ] **Step 1: demo.md**

Создать `demo.md` с демонстрацией: заголовки H1–H4, таблица, task-лист, цитата, код (js/python/bash), формула `$$\int_0^\infty e^{-x^2} dx = \frac{\sqrt{\pi}}{2}$$`, блок mermaid (flowchart + sequenceDiagram), `![альт](docs/img/example.png)` (создать `docs/img/example.png` — любой 1×1 PNG), HTML `<details>`, ссылки, зачёркивание, эмодзи.

- [ ] **Step 2: README.md**

README: название, скриншот-плейсхолдер (текстовая ссылка), возможности (GFM, Mermaid, KaTeX, TOC, поиск, темы, рендер/исходник, дерево папок), установка/запуск (`npm i`, `npm run dev`), сборка (`npm run build:win`), ассоциация .md (dev: `npm run register-assoc`; packaged: автоматически при установке), тесты (`npm run test`).

- [ ] **Step 3: Финальная проверка**

Run: `npm run test` — все зелёные.
Run: `npm run dev` и пройти чек-лист из спеки:

- [ ] рендер demo.md в обеих темах
- [ ] GFM: таблицы, task-листы, зачёркивание
- [ ] Mermaid-диаграмма отображается
- [ ] формулы KaTeX отображаются
- [ ] TOC-сайдбар: заголовки + скролл
- [ ] поиск подсвечивает, счётчик корректный
- [ ] переключатель рендер/исходник
- [ ] открытие файла и папки, дерево, клик по .md
- [ ] картинка с относительным путём из demo.md отображается
- [ ] drag&drop .md и папки
- [ ] `npm run build:win` собирает установщик

- [ ] **Step 4: Commit**

```bash
git add demo.md README.md docs/img
git commit -m "docs: full demo file and readme"
```

---

## Self-Review заметки

- **Покрытие спеки:** стек (T1), структура (T1), макет (T4–T8), конвейер (T2, T4, T5), открытие файлов/ассоциация (T3, T9), поиск (T7), темы (T4), безопасность (T1–T4), ошибки (T3, T8), тесты (T2, T5, T7), критерии готовности (T10).
- **Типы согласованы:** `FileContent { content, baseDir }` — Task 3, 4, 6; `TocEntry` — Task 5, 7; `highlightMatches` — Task 7; `renderMermaid` — Task 5.
- **Ожидаемые риски:** subpath-импорты CSS (T4 — фолбэк с копированием в assets), кириллица в slug (T2 — фолбэк поправить тест), размер mermaid (динамический import в T5).
